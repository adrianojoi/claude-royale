// Inglês — idioma padrão (canônico). Os nomes/descrições de carta vêm do
// catálogo (shared/cards.ts, já em inglês); aqui ficam só as strings de UI.
export const en = {
  ui: {
    // preenchido conforme as telas são migradas para t()
  },
};
